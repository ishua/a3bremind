# Фикс багов по результатам code review

> Исправление 6 валидированных багов средней важности из ревью `.opencode/temp/review.md`.

Без задачи — результат ревью кода.

## Контекст

В code review найдено 17 замечаний. После валидации 4 признаны false-positive,
8 — косметические, 6 — важные (IMPORTANT), требующих исправления.
Одно из них (#7 — UTC/timezone в done.go:91) уже исправлено по ходу обсуждения.
Остаётся 5 багов. План построен по принципу "от простого к сложному" внутри каждого слоя.

## Цель

Устранить 5 оставшихся багов средней важности, обеспечив:
- атомарность операций SetStatus + NextInstance для skip (#3/#8)
- атомарность AddMessageID + MarkMissedAndDeleteOnce для once reminders (#6)
- поддержку reply в done HH:MM (#9)
- персистентность БД в Docker (#15)
- логирование ошибок отправки Telegram (#17)

## Фаза 1: Транзакция в handleSkip + логирование ошибок sendText

- [x] (#7) Исправить вывод done_at в timezone пользователя (done.go:91) — сделано
- [x] (#3/#8) Обернуть SetStatus + NextInstance в handleSkip в транзакцию
  - Аналогично handleDone: `h.db.Begin()` → `store.SetStatus(tx, ...)` → `store.GetInstanceByID(tx, ...)` → `domain.NextInstance(tx, ...)` → `tx.Commit()`
  - При ошибке: `tx.Rollback()` (через defer)
- [x] (#17) sendText: логировать ошибку через `slog.Error` вместо игнорирования
  - handler.go:128: `if _, err := h.bot.Send(msg); err != nil { slog.Error(...) }`

В этой фазе два изолированных, точечных изменения в bot-слое. Не затрагивают сигнатуры и тесты.
> **Выполнено**: handleSkip обёрнут в транзакцию (tx.Begin/defer Rollback/Commit), sendText логирует ошибку через slog.Error.

## Фаза 2: Атомарность processInstance для once reminders

- [x] (#6) Создать в store/instance.go функцию `AddMessageIDAndMarkMissedDeleteOnce(db, instID, reminderID, messageID, sentAt)` аналогично существующей `AddMessageIDAndSetMissed`
  - Должна объединять `AddMessageID` + `MarkMissedAndDeleteOnce` в одной транзакции
- [x] Заменить два вызова в processInstance (pending.go:78-85) на один вызов новой функции
- [x] Добавить store-тесты для новой функции

Store-слой уже имеет функцию `AddMessageIDAndSetMissed` (для repeat != "once"). Новая функция нужна для проверки и удаления reminder.
> **Выполнено**: создана AddMessageIDAndMarkMissedDeleteOnce, заменены вызовы в processInstance, добавлены 2 store-теста (HappyPath, AlreadyDone).

## Фаза 3: done HH:MM reply support

- [x] (#9) В handleDoneWithTime (donewithtime.go:53-59): если есть ReplyToMessage, искать instance по message_id
  - Сейчас всегда fallback на `GetActiveByUser`
  - Нужно: если `update.Message.ReplyToMessage != nil` → `store.GetInstanceByMessageID(replyMsgID)`, иначе → `GetActiveByUser` (как сейчас)
- [x] Добавить проверку, что instance принадлежит пользователю (через `reminder.UserID == user.ID`)
- [x] Обновить тесты (bot_test.go): добавить кейс с reply + done HH:MM

> **Выполнено**: handleDoneWithTime поддерживает reply (GetInstanceByMessageID + проверка владельца), добавлен TestHandleDone_WithTime_Reply.

## Фаза 4: Docker persistence

- [x] (#15) Добавить в main.go поддержку пути к БД через переменную окружения
  - `dbPath := os.Getenv("DB_PATH")` с fallback `"bot.db"`
- [x] В Dockerfile: создать директорию `/data`
  - `RUN mkdir /data`
- [x] В docker-compose.yml: добавить volume mount
  - `volumes: - ./data:/data`
  - Переменная окружения: `DB_PATH=/data/bot.db`
> **Выполнено**: main.go читает DB_PATH (fallback bot.db), Dockerfile создаёт /data, docker-compose.yml монтирует volume ./data:/data и задаёт DB_PATH=/data/bot.db.

## Решения и договорённости

- **Транзакция для handleSkip**: сделано по аналогии с handleDone. handleSnooze не требует транзакции — scheduled_at сдвигается в будущее, двойной отправки не будет (reviewer ошибался).
- **done HH:MM reply**: проверяем владельца instance через reminder.UserID, хотя в 1:1 боте это скорее защита от ошибок, чем от атак.
- **processInstance once**: новая store-функция вместо транзакции в domain-слое — паттерн уже используется для `AddMessageIDAndSetMissed`.
- **go.mod 1.26.3**: не трогаем — на June 2026 версия валидна.
- **23:59:59 в Reschedule**: не трогаем — код корректен, `last.After(23:59:59)` верно ловит время после полуночи (00:00:00 > 23:59:59).

## Открытые вопросы

> Вопросов нет — все 17 находок валидированы и распределены по фазам.