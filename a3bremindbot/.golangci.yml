version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - unconvert
    - dupl
    - misspell

formatters:
  enable:
    - gofmt

run:
  timeout: 5m
  tests: true


